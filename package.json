{
  "name": "lottery-wheels",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@lottery-wheels/frontend",
    "dev:backend": "npm run dev --workspace=@lottery-wheels/backend",
    "dev:all": "npm run dev:backend & npm run dev",
    "build": "npm run build --workspace=@lottery-wheels/frontend",
    "lint": "npm run lint --workspace=@lottery-wheels/frontend"
  }
}
